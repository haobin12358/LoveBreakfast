<style scoped lang="less" rel="stylesheet/less">
  @import "../styles/common";
  .my-card{
    width: 500rpx;
    border-radius: 10px;
    text-align: center;
    padding: 20rpx 0;
    background-color: #999999;
    color: #fff;
    font-size: 24rpx;
    margin-bottom: 20rpx;
    &.m-active{
      background-color: @main_color;
    }
    .my-title{
      font-size: 38rpx;
      margin-bottom: 20rpx;
    }
  }
</style>
<template>
  <view class="my-card {{choose && (choose.CAid == list.CAid)?'m-active':''}}" catchtap="cardTap({{list}})">
    <view class="my-title" >{{list.COcut }}</view>
    <view>
      <span>{{list.COuse}}</span>  限{{list.COstart}}-{{list.COend}}使用</view>
  </view>
</template>
<script>
    import wepy from 'wepy';

    export default class Card extends wepy.page {
        props={
          list:{
            type:Object,
            default:null
          },
          choose:{
            type:Object,
            default:null
          }
        }
        data = {}
        methods = {
          cardTap(v){
            console.log(v);
            this.$emit('cardTap',v)
          }
        }
    }
</script>

